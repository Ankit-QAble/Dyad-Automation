---
product: nexsure
journey: new-business
pages: [login, policy-quote]
---

1. Log in as a standard agent user (`NEXSURE_LOGIN_USER` / `NEXSURE_LOGIN_PASS`).
2. Start a new business quote for a personal auto policy.
3. Enter the minimum required applicant and vehicle details to reach a quote result.
4. Confirm a quote premium is displayed and the quote can be saved.
5. Log out.

<!--
This scenario is a starting example. The generator agent resolves `pages: [login,
policy-quote]` against nexsure/knowledge/registry.yaml — those entries (and their
underlying knowledge files) must exist and be populated before a test case can be
generated from this scenario. Run agents/explorer-agent.ts against Nexsure first.
-->
